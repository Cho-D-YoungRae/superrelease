# M11 정합 잔여 + 프로즈 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** P1 마지막 마일스톤 — Cargo.lock 동기화(B-9), per-package CHANGELOG 목적지(B-10), watchPaths(B-11), B-19 프로즈 잔여 10항목 + M8~M10 이월 정리를 랜딩한다.

**Architecture:** 스펙은 [2026-08-13-m11-consistency-prose-design.md](../specs/2026-08-13-m11-consistency-prose-design.md). 스크립트 확장 2건(sync_package_lock·path_prefix의 대칭 확장 — 산술 무변경) + validate 규칙 2건 + asset 게이트/프로즈 + scan 다듬기 3건 + init/references 프로즈. 골든 blast radius: 전 골든 version.py + (asset 프로즈로) 전 골든 release SKILL.md + 모노레포 골든 changed-packages.py·release SKILL.md + 신규 `package-changelog-monorepo` 트리(33종).

**Tech Stack:** Python 3.9+ stdlib · unittest · 동결 template dialect

**실행 모델:** 구현 서브에이전트는 **opus 모델**로 dispatch한다(사용자 지시).

## Global Constraints

- 테스트 러너: 전체 `python3 -m unittest discover -s tests -q`. 단일 모듈 `cd tests && python3 -m unittest <mod> -v; cd ..`. **dotted 형식 금지(ModuleNotFoundError).**
- 스크립트(version.py·changed-packages.py)는 Python 3.9+ 표준 라이브러리만, exit 0/1/2 규약 유지. **버전 산술은 건드리지 않는다.**
- 조건 블록은 그 기능이 없는 config에서 0바이트 collapse(공백·개행은 `{{#if}}` 안). 골든 재생성은 Task 5에서 한 번 — Task 1~4는 골든·dogfood 테스트가 실패한 채 남는 것이 정상이다(각 태스크의 gate는 자기 모듈 테스트).
- validate 메시지·테스트 어서션은 규칙 고유 문구 핀.
- 프로즈 한국어, 커밋 Conventional Commits, 트레일러 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 작업 브랜치 `feat/m11-consistency-prose`(체크아웃됨). push·PR 금지.
- `version.py`의 `location_pattern`은 re.MULTILINE로 컴파일한다 — config에 들어가는 regex는 `^` 앵커 사용 가능. scan.py 쪽에서 같은 패턴을 쓸 때는 `re.search(..., re.M)`을 명시해야 한다.

---

### Task 1: B-9 — sync_cargo_lock (version.py)

**Files:**
- Modify: `skills/init/assets/scripts/version.py` (`sync_package_lock` 아래 신규 함수 + `set_location` regex 분기)
- Test: `tests/test_version.py`

**Interfaces:**
- Produces: `sync_cargo_lock(toml_path, new_version)` — Cargo.toml regex 위치 set 시 자동 호출, 출력 `Cargo.lock: synced to <v>`. Task 5의 골든 재생성이 이 파일의 verbatim 복사를 갱신한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_version.py`에 추가(파일 기존 스타일: 모듈 상단 픽스처 상수 + `helpers.make_repo`/`run_script` — 파일을 열어 기존 클래스·헬퍼 사용 방식을 확인하고 그 관례에 맞춰 배치하라. 아래 코드의 픽스처·어서션은 그대로 쓴다):

```python
CARGO_TOML = ('[package]\nname = "demo-cli"\nversion = "1.2.0"\n'
              'edition = "2021"\n\n[dependencies]\nserde = "1"\n')
CARGO_LOCK = ('# This file is automatically @generated by Cargo.\n'
              'version = 3\n\n'
              '[[package]]\nname = "demo-cli"\nversion = "1.2.0"\n'
              'dependencies = [\n "serde",\n]\n\n'
              '[[package]]\nname = "serde"\nversion = "1.0.200"\n')


class CargoLockSyncTest(unittest.TestCase):
    def _repo(self, files):
        tmp = tempfile.TemporaryDirectory()
        self.addCleanup(tmp.cleanup)
        cfg = scope_config([{"file": "Cargo.toml", "type": "regex",
                             "pattern": "^version\\s*=\\s*\\\"([^\\\"]+)\\\""}])
        cfg["scopes"][0]["preRelease"] = {"style": "none", "qualifier": None}
        cfg["scopes"][0]["postRelease"] = {"bump": "none"}
        repo = make_repo(tmp.name, cfg, files)
        return repo, repo / ".superrelease" / "scripts" / "version.py"

    def test_set_syncs_own_cargo_lock_entry_only(self):
        repo, vp = self._repo({"Cargo.toml": CARGO_TOML,
                               "Cargo.lock": CARGO_LOCK})
        r = run_script(vp, "set", "1.3.0", cwd=repo)
        self.assertEqual(r.returncode, 0, r.stderr)
        self.assertIn("Cargo.lock: synced to 1.3.0", r.stdout)
        lock = (repo / "Cargo.lock").read_text()
        self.assertIn('name = "demo-cli"\nversion = "1.3.0"', lock)
        self.assertIn('name = "serde"\nversion = "1.0.200"', lock)  # 미오염

    def test_set_without_lock_file_skips_quietly(self):
        repo, vp = self._repo({"Cargo.toml": CARGO_TOML})
        r = run_script(vp, "set", "1.3.0", cwd=repo)
        self.assertEqual(r.returncode, 0, r.stderr)
        self.assertNotIn("Cargo.lock", r.stdout)

    def test_set_without_own_entry_skips_quietly(self):
        lock_no_self = ('version = 3\n\n[[package]]\nname = "serde"\n'
                        'version = "1.0.200"\n')
        repo, vp = self._repo({"Cargo.toml": CARGO_TOML,
                               "Cargo.lock": lock_no_self})
        r = run_script(vp, "set", "1.3.0", cwd=repo)
        self.assertEqual(r.returncode, 0, r.stderr)
        self.assertNotIn("synced", r.stdout)
        self.assertIn('version = "1.0.200"', (repo / "Cargo.lock").read_text())
```

(import에 `scope_config`·`make_repo`·`tempfile`이 없으면 추가.)

- [ ] **Step 2: 실패 확인**

Run: `cd tests && python3 -m unittest test_version.CargoLockSyncTest -v; cd ..`
Expected: `test_set_syncs_own_cargo_lock_entry_only` FAIL(synced 출력 없음). 나머지 2건은 기존 코드에서도 통과할 수 있다(동기화가 없으니) — 1번의 RED가 핵심.

- [ ] **Step 3: version.py 구현**

(a) `sync_package_lock` 함수 바로 아래에 추가:

```python
def sync_cargo_lock(toml_path, new_version):
    """Mirror of sync_package_lock for Cargo: rewrite only this package's
    [[package]] entry in the sibling Cargo.lock (cargo's generated layout
    keeps name/version on adjacent lines). Dependency entries are never
    touched; missing lock or missing entry is a quiet no-op."""
    lock = toml_path.parent / "Cargo.lock"
    if not lock.is_file():
        return
    ttext, _ = read_text_preserving(toml_path)
    sec = re.search(r"^\[package\]\s*$(.*?)(?=^\[|\Z)", ttext, re.M | re.S)
    name_m = re.search(r"^name\s*=\s*\"([^\"]+)\"", sec.group(1), re.M) \
        if sec else None
    if not name_m:
        return
    text, crlf = read_text_preserving(lock)
    pat = re.compile(r"(\[\[package\]\]\s*\nname = \""
                     + re.escape(name_m.group(1))
                     + r"\"\s*\nversion = \")([^\"]+)(\")")
    if not pat.search(text):
        return
    text = pat.sub(lambda m: m.group(1) + new_version + m.group(3), text,
                   count=1)
    write_text_preserving(lock, text, crlf)
    print("Cargo.lock: synced to " + new_version)
```

(b) `set_location`의 regex 분기에서 `write_text_preserving(path, text, crlf)` 직후 · `return old` 직전에 추가:

```python
        if path.name == "Cargo.toml":
            sync_cargo_lock(path, new_version)
```

(파일 상단에 `re` import는 이미 있다 — 확인만.)

- [ ] **Step 4: 통과 확인**

Run: `cd tests && python3 -m unittest test_version -v 2>&1 | tail -5; cd ..`
Expected: 전부 PASS.

- [ ] **Step 5: 커밋**

```bash
git add skills/init/assets/scripts/version.py tests/test_version.py
git commit -m "feat(scripts): Cargo.lock 동기화 — set 시 자기 패키지 항목만 갱신 (B-9)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: B-11 — changed-packages watchPaths

**Files:**
- Modify: `skills/init/assets/scripts/changed-packages.py` (`changed_for`)
- Modify: `skills/init/scripts/render.py` (`validate_config` scope 루프)
- Modify: `tests/helpers.py` (scope에 `"watchPaths": []`)
- Test: `tests/test_changed_packages.py`, `tests/test_render_pipeline.py`

**Interfaces:**
- Produces: scope 선택 필드 `watchPaths`(문자열 배열) — scope 경로 밖 공유 경로 변경을 그 scope 변경으로 집계. validate 메시지 `scopes[i].watchPaths must be a list of non-empty strings`. Task 7의 init 프로즈·Task 8의 README가 이 필드를 인용한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_changed_packages.py`의 `ChangedPackagesTest`에 추가(기존 setUp이 `monorepo_config()` + packages/a·b 레포를 만든다 — 이 테스트는 자체 config로 별도 레포를 만든다):

```python
    def test_watch_paths_pull_shared_changes_into_scope(self):
        tmp = tempfile.TemporaryDirectory()
        self.addCleanup(tmp.cleanup)
        repo = make_git_repo(tmp.name, files={
            "packages/a/package.json": PKG_A,
            "packages/b/package.json": PKG_B,
            "shared/util.js": "x\n",
        }, commits=["feat: init"], tags=["a@0.1.0", "b@0.1.0"])
        cfg = monorepo_config()
        cfg["scopes"][0]["watchPaths"] = ["shared/"]
        make_repo(repo, cfg, {})
        write(repo / "shared" / "util.js", "y\n")
        g(repo, "add", "-A")
        g(repo, "commit", "-q", "-m", "fix: shared util")
        r = run_script(repo / ".superrelease" / "scripts" / "changed-packages.py",
                       "--json")
        self.assertEqual(r.returncode, 0, r.stderr)
        scopes = {s["name"]: s for s in json.loads(r.stdout)["scopes"]}
        self.assertTrue(scopes["a"]["hasChanges"])   # watchPaths로 포착
        self.assertFalse(scopes["b"]["hasChanges"])  # watch 없음 — 불변

    def test_watch_paths_included_on_first_release_listing(self):
        tmp = tempfile.TemporaryDirectory()
        self.addCleanup(tmp.cleanup)
        repo = make_git_repo(tmp.name, files={
            "packages/a/package.json": PKG_A,
            "packages/b/package.json": PKG_B,
            "shared/util.js": "x\n",
        }, commits=["feat: init"])  # 태그 없음 — anchor 부재
        cfg = monorepo_config()
        cfg["scopes"][0]["watchPaths"] = ["shared/"]
        make_repo(repo, cfg, {})
        r = run_script(repo / ".superrelease" / "scripts" / "changed-packages.py",
                       "--json")
        scopes = {s["name"]: s for s in json.loads(r.stdout)["scopes"]}
        self.assertIn("shared/util.js", scopes["a"]["changed"])
        self.assertNotIn("shared/util.js", scopes["b"]["changed"])
```

`tests/test_render_pipeline.py`에 validate 테스트:

```python
    def test_watch_paths_rejects_non_string_list(self):
        render_mod = load_module(PLUGIN_SCRIPTS / "render.py", "render_wp")
        cfg = scope_config([{"file": "x", "type": "regex", "pattern": "v(1)"}])
        cfg["scopes"][0]["watchPaths"] = ["shared/", 7]
        problems = render_mod.validate_config(cfg)
        self.assertTrue(any("watchPaths must be a list of non-empty strings"
                            in p for p in problems), problems)

    def test_watch_paths_absent_and_valid_pass(self):
        render_mod = load_module(PLUGIN_SCRIPTS / "render.py", "render_wp2")
        for wp in (None, [], ["shared/"]):
            with self.subTest(watchPaths=wp):
                cfg = scope_config([{"file": "x", "type": "regex",
                                     "pattern": "v(1)"}])
                if wp is not None:
                    cfg["scopes"][0]["watchPaths"] = wp
                self.assertEqual(render_mod.validate_config(cfg), [])
```

- [ ] **Step 2: 실패 확인**

Run: `cd tests && python3 -m unittest test_changed_packages.ChangedPackagesTest.test_watch_paths_pull_shared_changes_into_scope test_render_pipeline -v 2>&1 | tail -6; cd ..`
Expected: watch 테스트 FAIL(`hasChanges` False) + validate 거부 테스트 FAIL.

- [ ] **Step 3: 구현**

(a) `changed-packages.py`의 `changed_for`를 교체:

```python
def changed_for(scope, ref):
    anchor, kind = resolve_anchor(scope, ref)
    prefix = path_prefix(scope.get("path"))
    watch = [p for p in (path_prefix(w) for w in scope.get("watchPaths") or [])
             if p]
    if anchor:
        out = git("diff", "--name-only", "--no-renames", anchor + "..HEAD")
    else:
        out = git("ls-files", "--", scope.get("path") or ".",
                  *(scope.get("watchPaths") or []))
    files = [f for f in out.splitlines() if f.strip()]
    if prefix:
        prefixes = [prefix] + watch
        files = [f for f in files if any(f.startswith(p) for p in prefixes)]
    return {"name": scope.get("name"), "path": scope.get("path"),
            "anchor": anchor, "anchorType": kind,
            "hasChanges": bool(files), "changedCount": len(files),
            "changed": files}
```

(루트 scope(`path: "."` → prefix `""`)는 종전대로 무필터 — watchPaths가 무의미하므로 동작 불변.)

(b) `render.py` `validate_config`의 scope 루프(예: `dependents` 검증 근처)에 추가:

```python
        watch = s.get("watchPaths")
        if watch is not None and (not isinstance(watch, list) or any(
                not isinstance(w, str) or not w for w in watch)):
            problems.append("scopes[{}].watchPaths must be a list of "
                            "non-empty strings (extra paths whose changes "
                            "count for this scope)".format(i))
```

(c) `tests/helpers.py`의 `scope_config` scope dict에서 `"dependents": [],` 앞에 `"watchPaths": [],` 추가. `monorepo_config`의 `pkg_scope`에도 `"dependents": dependents,` 앞에 `"watchPaths": [],` 추가.

- [ ] **Step 4: 통과 확인**

Run: `cd tests && python3 -m unittest test_changed_packages test_render_pipeline -v 2>&1 | tail -5; cd ..`
Expected: 전부 PASS.

- [ ] **Step 5: 커밋**

```bash
git add skills/init/assets/scripts/changed-packages.py skills/init/scripts/render.py tests/helpers.py tests/test_changed_packages.py tests/test_render_pipeline.py
git commit -m "feat(scripts): scope watchPaths — 공유 경로 변경을 scope 변경으로 집계 (B-11)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: B-10 — package-changelog 목적지 (validate + derived + 게이트)

**Files:**
- Modify: `skills/init/scripts/render.py` (`known_dests`·independent 한정 규칙·`derived_flags`)
- Modify: `skills/init/assets/skills/release-monorepo/SKILL.md` (§5)
- Test: `tests/test_render_pipeline.py`, `tests/test_assets.py`

**Interfaces:**
- Consumes: 기존 `derived_flags`·`any_dest` 구조.
- Produces: destinations 값 `"package-changelog"`, derived 플래그 `anyNotesPackageChangelog`, 렌더 고유 문구 "`package-changelog`가 그 scope의 목적지면" — Task 5의 신규 골든이 핀한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_render_pipeline.py`:

```python
    def test_package_changelog_requires_independent(self):
        render_mod = load_module(PLUGIN_SCRIPTS / "render.py", "render_pc")
        cfg = scope_config([{"file": "x", "type": "regex", "pattern": "v(1)"}])
        cfg["scopes"][0]["notes"]["destinations"] = ["package-changelog",
                                                     "github-release"]
        problems = render_mod.validate_config(cfg)
        self.assertTrue(any('"package-changelog" requires monorepoStrategy '
                            '"independent"' in p for p in problems), problems)

    def test_package_changelog_accepted_on_independent(self):
        render_mod = load_module(PLUGIN_SCRIPTS / "render.py", "render_pc2")
        cfg = monorepo_config()
        for s in cfg["scopes"]:
            s["notes"]["destinations"] = ["package-changelog", "github-release"]
        self.assertEqual(render_mod.validate_config(cfg), [])
```

`tests/test_assets.py`(`MonorepoAssetsTest`):

```python
    def test_release_monorepo_package_changelog_gate(self):
        # B-10: package-changelog는 쓰는 config에서만 §5에 렌더된다.
        pc_ctx = mono_ctx()
        for s in pc_ctx["scopes"]:
            s["notes"]["destinations"] = ["package-changelog", "github-release"]
        pc_ctx["derived"] = render.derived_flags(pc_ctx["scopes"])
        out = self.render_asset("skills/release-monorepo/SKILL.md", pc_ctx)
        self.assertIn("`package-changelog`가 그 scope의 목적지면", out)

        default_out = self.render_asset("skills/release-monorepo/SKILL.md")
        self.assertNotIn("package-changelog", default_out)
```

- [ ] **Step 2: 실패 확인**

Run: `cd tests && python3 -m unittest test_render_pipeline test_assets.MonorepoAssetsTest -v 2>&1 | tail -6; cd ..`
Expected: 신규 3건 FAIL(거부 규칙 없음 → unknown destination 메시지로는 통과 못 함·게이트 문구 없음). 주의: 첫 테스트는 현재 "unknown notes destination" 메시지가 나와 assertTrue가 실패하는 형태의 RED다.

- [ ] **Step 3: 구현**

(a) `render.py`: `known_dests = {"changelog", "release-file", "github-release", "fragment"}` → `{"changelog", "release-file", "github-release", "fragment", "package-changelog"}`. unknown 메시지의 supported 목록에 `package-changelog` 추가. `sinks = known_dests - {"fragment"}` 그대로(package-changelog는 sink로 인정됨). 그리고 dests 루프 뒤에:

```python
        if ("package-changelog" in dests
                and repo.get("monorepoStrategy") != "independent"):
            problems.append('scopes[{}]: notes destination "package-changelog" '
                            'requires monorepoStrategy "independent" — on a '
                            'single-version repo use "changelog" (the root '
                            "changelog is the package changelog)".format(i))
```

(b) `derived_flags`의 return dict에 `"anyNotesPackageChangelog": any_dest("package-changelog"),` 추가.

(c) `release-monorepo/SKILL.md` §5의 `{{#if derived.anyNotesChangelog}}…{{/if}}` 줄 바로 뒤에(같은 구조·개행은 게이트 안):

```
{{#if derived.anyNotesPackageChangelog}}- `package-changelog`가 그 scope의 목적지면: 그 scope 경로의 `CHANGELOG.md`(`<scope.path>/CHANGELOG.md`) 최신 항목으로 삽입 — `.superrelease/templates/changelog-entry.md` 골격을 쓰되 헤딩은 scope 파일 안이므로 `## <version>`(루트 CHANGELOG의 `## <scope>@<version>`과 구분). 파일이 없으면 Keep a Changelog 골격으로 신설하라.
{{/if}}
```

- [ ] **Step 4: 통과 확인**

Run: `cd tests && python3 -m unittest test_render_pipeline test_assets -v 2>&1 | tail -5; cd ..`
Expected: 전부 PASS(골든·dogfood 테스트는 이 태스크 게이트 밖 — 전체 스위트는 Task 5까지 실패 상태가 정상).

- [ ] **Step 5: 커밋**

```bash
git add skills/init/scripts/render.py skills/init/assets/skills/release-monorepo/SKILL.md tests/test_render_pipeline.py tests/test_assets.py
git commit -m "feat(notes): package-changelog 목적지 — independent 전용 validate + §5 게이트 (B-10)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: asset 프로즈 정리 — preflight 문구·결번·회수 어휘

**Files:**
- Modify: `skills/init/assets/skills/release/SKILL.md` (preflight 도입부·실패 절)
- Modify: `skills/init/assets/skills/release-monorepo/SKILL.md` (preflight 도입부·preflight 6 문구·실패 절이 있으면)
- Test: `tests/test_assets.py` (기존 어서션 깨짐 여부 확인·필요시 갱신)

**Interfaces:**
- Produces: 무게이트 공통 프로즈 변경 — Task 5의 골든 재생성에서 **모든 골든의 release SKILL.md가 변한다**(예상 blast radius).

- [ ] **Step 1: preflight 결번 한 줄(양 asset)**

`release/SKILL.md`의 `## 1. preflight — 모두 통과해야 진행` 헤딩 바로 아래 줄에(빈 줄 다음, 목록 시작 전) 추가:

```
(항목 번호는 식별용이다 — 설정에 없는 항목은 통째로 사라지므로 결번이 보여도 정상이다.)
```

`release-monorepo/SKILL.md`의 preflight 헤딩 아래에도 같은 문장을 추가한다(헤딩 문구가 다르면 그 헤딩 기준 — 목록 시작 전).

- [ ] **Step 2: 모노레포 preflight 6 인접 문구 명료화**

`release-monorepo/SKILL.md`의 trunk 분기(else)에서:

- `태그를 쓰지 않는 scope는 이 검사를 건너뛴다.` → `태그를 쓰지 않는 scope(config \`tag.enabled\` false)는 이 검사를 건너뛴다.`
- `anchor 태그가 **하나도 없는** scope는 파일 버전 기반 감지가 불가능하다` → `아직 릴리스 태그가 한 번도 만들어지지 않은 scope(anchor 부재 — tagless 설정과 다르다)는 파일 버전 기반 감지가 불가능하다`

(둘 다 라인 32의 같은 문장 안 — Edit로 각각 정확 치환.)

- [ ] **Step 3: 회수 어휘 확장(단일 release 실패 절 + 모노레포에 있으면 동일)**

`release/SKILL.md` 실패 절의:

```
잘못 나간 버전은 다음 패치로 덮고, 배포물 회수는 생태계 절차(npm deprecate, PyPI yank 등)를 안내하라.
```

을 다음으로 교체:

```
잘못 나간 버전은 다음 패치로 덮고, 배포물 회수는 생태계 절차를 안내하라 — npm deprecate · PyPI yank · cargo yank · Go retract(모듈에 retract 지시문 추가 후 새 패치 발행) · 모바일 스토어는 롤백 불가(수정판 롤포워드, 필요시 단계 출시 중단은 콘솔에서) · 데스크톱 업데이터 피드는 직전 정상 버전 재게시 · 컨테이너 릴리스 태그는 재푸시 금지(새 패치 태그로).
```

`release-monorepo/SKILL.md`에 같은 실패 절 문장이 있으면 동일 교체, 없으면 건너뛴다(리포트에 어느 쪽인지 기록).

- [ ] **Step 4: test_assets 확인 + 커밋**

Run: `cd tests && python3 -m unittest test_assets -v 2>&1 | tail -5; cd ..`
Expected: 기존 어서션 중 이 문구들을 핀한 것이 있으면 깨진다(예: 실패 절 문구) — 새 문구로 갱신하고 사유를 커밋 메시지에 남겨라. 그 외 전부 PASS.

```bash
git add skills/init/assets/skills/release/SKILL.md skills/init/assets/skills/release-monorepo/SKILL.md tests/test_assets.py
git commit -m "docs(assets): preflight 결번 안내·tagless/anchor 문구 구분·회수 어휘 확장 (B-19)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 골든 재생성 + package-changelog-monorepo 신설 + dogfood

**Files:**
- Modify: `tests/golden_configs.py`
- Modify: `tests/golden/**` (재생성) · `.claude/skills/release/SKILL.md`·`.superrelease/scripts/*`(dogfood)

**Interfaces:**
- Consumes: Task 1~4의 전 변경.

- [ ] **Step 1: golden_configs.py에 추가**

`mixed_tags_monorepo()` 뒤에:

```python
def package_changelog_monorepo():
    cfg = monorepo_config()
    for s in cfg["scopes"]:
        s["notes"]["destinations"] = ["package-changelog", "github-release"]
    return cfg
```

`GOLDEN` dict 끝에 `"package-changelog-monorepo": package_changelog_monorepo,` 추가.

- [ ] **Step 2: 골든 재생성 + 범위 확인**

```bash
python3 tests/update_golden.py
git status --porcelain tests/golden | head -60
git status --porcelain tests/golden | wc -l
```

Expected 변경: ① 전 골든의 `.superrelease/scripts/version.py`(Task 1) ② 모노레포 골든의 `.superrelease/scripts/changed-packages.py`(Task 2) ③ 전 골든의 release/SKILL.md(Task 4 결번·회수 어휘 — 모노레포 골든은 §5·preflight 문구 포함) ④ 신규 `tests/golden/package-changelog-monorepo/` 트리. **그 밖의 파일(templates·release-notes·hotfix·backfill 등 프로즈 무변경 파일)이 바뀌면 회귀 — 중단·보고.** (hotfix·backfill SKILL.md는 Task 4가 건드리지 않았으므로 불변이어야 한다.)

- [ ] **Step 3: dogfood 재렌더 + 전체 검증**

```bash
NOW=$(python3 -c "import json; print(json.load(open('.superrelease/config.json'))['superrelease']['generatedAt'])")
python3 skills/init/scripts/render.py --config .superrelease/config.json --assets skills/init/assets --repo . --now "$NOW"
git status --porcelain .claude .superrelease
python3 -m unittest discover -s tests -q 2>&1 | tail -3
claude plugin validate . --strict 2>&1 | tail -1
```

Expected: dogfood 변경은 `.superrelease/scripts/version.py`·`.superrelease/scripts/changed-packages.py`·`.claude/skills/release/SKILL.md`뿐(superrelease는 단일 레포 — release-monorepo 미렌더) / 전체 스위트 OK / Validation passed.

- [ ] **Step 4: 커밋**

```bash
git add tests/golden_configs.py tests/golden .claude .superrelease
git commit -m "test(golden): M11 재baseline — 스크립트 2종·asset 프로즈 + package-changelog-monorepo 신설(33종)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: scan 다듬기 — [project] 스코프·charts dedup·versionName 앵커

**Files:**
- Modify: `skills/init/scripts/scan.py`
- Test: `tests/test_scan.py`

**Interfaces:**
- Produces: `ANDROID_VERSION_NAME_PATTERN = "^\\s*versionName\\s*=?\\s*['\\\"]([^'\\\"]+)['\\\"]"`(캡처 1개 유지 — 패턴 전수 테스트 하한 14 불변). 골든·dogfood 무관.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_scan.py`에 추가:

```python
class ScanRefinementTest(unittest.TestCase):
    def test_python_deps_scoped_to_project_table(self):
        # [tool.x] 테이블의 행-시작 dependencies 배열을 오탐하지 않는다.
        report = scan_tmp(self, {
            "pyproject.toml":
                '[project]\nname = "root"\nversion = "0.0.0"\n\n'
                '[tool.uv.workspace]\nmembers = ["packages/*"]\n',
            "packages/api/pyproject.toml":
                '[project]\nname = "demo-api"\nversion = "1.0.0"\n\n'
                '[tool.custom]\ndependencies = ["demo-core"]\n',
            "packages/core/pyproject.toml":
                '[project]\nname = "demo-core"\nversion = "1.0.0"\n'})
        self.assertEqual(report["monorepo"]["internalDependencies"], [])

    def test_charts_child_that_is_node_package_not_duplicated(self):
        report = scan_tmp(self, {
            "pnpm-workspace.yaml": 'packages:\n  - "charts/*"\n',
            "charts/web/package.json": '{"name": "web", "version": "1.0.0"}\n',
            "charts/web/Chart.yaml": "apiVersion: v2\nname: web\nversion: 0.1.0\n"})
        paths = [p["path"] for p in report["monorepo"]["packages"]]
        self.assertEqual(paths.count("charts/web"), 1)

    def test_commented_version_name_not_matched(self):
        report = scan_tmp(self, {
            "app/build.gradle":
                'android {\n  defaultConfig {\n'
                '    // versionName "9.9.9"\n'
                '    versionName "3.1.0"\n  }\n}\n'})
        cands = [c for c in report["versionCandidates"]
                 if c["file"] == "app/build.gradle"]
        self.assertEqual(cands[0]["value"], "3.1.0")
```

- [ ] **Step 2: 실패 확인**

Run: `cd tests && python3 -m unittest test_scan.ScanRefinementTest -v; cd ..`
Expected: 3건 FAIL(내부 의존성 오탐·중복 항목·주석 선매치 9.9.9).

- [ ] **Step 3: 구현**

(a) `_python_packages`의 deps 수집에서 `dep_m = re.search(r"^dependencies\s*=\s*\[(.*?)\]", ptext, re.M | re.S)` 를 `[project]` 섹션으로 한정:

```python
            proj = re.search(r"^\[project\]\s*$(.*?)(?=^\[|\Z)",
                             ptext, re.M | re.S)
            dep_m = re.search(r"^dependencies\s*=\s*\[(.*?)\]",
                              proj.group(1) if proj else "", re.M | re.S)
```

(b) `scan_monorepo`의 charts 블록에서 append 전에 dedup 가드:

```python
            existing = {p["path"] for p in packages}
            for d in chart_children:
                rel = d.relative_to(repo).as_posix()
                if rel in existing:
                    continue
                ...
```

(기존 루프를 이 형태로 감싼다 — signals.append는 chart_children 존재 기준 그대로.)

(c) `ANDROID_VERSION_NAME_PATTERN`을 `"^\\s*versionName\\s*=?\\s*['\\\"]([^'\\\"]+)['\\\"]"` 로 교체하고, 이 패턴을 쓰는 `re.search(ANDROID_VERSION_NAME_PATTERN, text)` 호출에 `re.M` 플래그를 추가한다(주석 줄은 `//`가 `\s*` 뒤에 오므로 불매치; `^` 앵커는 version.py의 re.MULTILINE 컴파일과 호환).

- [ ] **Step 4: 통과 확인**

Run: `cd tests && python3 -m unittest test_scan test_render_pipeline -v 2>&1 | tail -5; cd ..`
Expected: 전부 PASS(패턴 전수 테스트 — 새 상수 없음, 하한 14 유지).

- [ ] **Step 5: 커밋**

```bash
git add skills/init/scripts/scan.py tests/test_scan.py
git commit -m "fix(scan): [project] 스코프 deps·charts dedup·versionName 주석 선매치 앵커 (M11 이월)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: init SKILL.md + references 프로즈 (B-19 잔여 + 이월)

**Files:**
- Modify: `skills/init/SKILL.md`
- Modify: `skills/init/references/{edge-cases,notes-and-changelog,monorepo,prerelease-and-dev-channel,version-schemes}.md`

전부 플러그인 프로즈(렌더 무관). 각 항목의 **삽입 위치 앵커와 텍스트**를 그대로 적용하라. old 인용이 파일과 다르면 NEEDS_CONTEXT로 중단·보고.

- [ ] **Step 1: init SKILL.md — 8건**

1. **번들 5 목적지 추가**: `destinations 복수 선택(changelog | release-file | github-release | fragment)` → `destinations 복수 선택(changelog | release-file | github-release | fragment | package-changelog(independent 모노레포 전용 — scope 경로의 CHANGELOG.md에 scope별 이력))`
2. **번들 1 watchPaths 한 줄**: 번들 1의 independent scope 확정 문장(`…scope 목록(이름 = 패키지 이름 또는 디렉터리명, path, 버전 파일)을 확정한다`) 뒤에 이어서: ` 이때 전 scope에 영향을 주는 공유 경로(공용 라이브러리·codegen 소스 등)가 있으면 영향받는 scope의 \`watchPaths\`에 기록하라(그 경로의 변경이 그 scope의 변경으로 집계된다).`
3. **번들 6 mergePolicy unknown**: 번들 6 끝에 ` / mergePolicy — 팀에 정해진 머지 정책이 없으면 \`unknown\`으로 기록하라(닫힌 집합 merge|squash|rebase|unknown — 오타는 render가 거부한다)` 추가.
4. **번들 4 PEP 440 트리거 보강**: `pyproject.toml이 버전 후보에 있으면` → `buildSystems에 python이 있거나 pyproject.toml이 버전 후보에 있으면`
5. **번들 4 빌드 번호 트리거 완화(이월)**: `buildSystems에 \`flutter\`/\`dart\`가 있거나,` → `buildSystems에 \`flutter\`가 있거나,` (bare dart 라이브러리 과대 트리거 제거 — xcconfig·versionName·buildNumber 신호 조건은 그대로).
6. **번들 4/5 scope별 차등 한 줄**: 번들 4 항목 서두 `수식어 스타일` 앞에 `(independent 모노레포는 번들 4·5를 scope별로 다르게 답할 수 있다 — pre/post·notes는 scope 필드다) ` 추가.
7. **번들 7 초기 버전 상호참조(이월)**: `기존 버전이 없으면 0.1.0 vs 1.0.0(공개 API 안정성 약속 기준으로 설명)` → `기존 버전이 없으면 0.1.0 vs 1.0.0(공개 API 안정성 약속 기준으로 설명 — 번들 2의 후보 0건·placeholder 시드 규칙과 같은 결정이다: 최신 릴리스 태그가 있으면 그 버전이 우선)`. 그리고 번들 2의 `제안값은 후보 0건 분기와 같은 규칙(최신 릴리스 태그 우선, 없으면 0.1.0(초기 개발)/1.0.0(이미 안정) 중 선택)이며` → `제안값은 최신 릴리스 태그 우선, 없으면 번들 7의 첫 릴리스 질문(0.1.0/1.0.0)과 같은 기준으로 고르게 하며` (superset 표현 정정).
8. **Phase 3 모순 정정**: `## config.json 작성` 헤딩 바로 아래에 `(이 작성은 Phase 3(생성)의 첫 단계다 — "Phase 3 전에는 어떤 파일도 만들지 않는다" 규칙과 일관되게, 모든 결정이 끝난 뒤에만 쓴다.)` 한 줄 추가.
9. **번들 3 compatCheck JVM 한정(이월)**: `(라이브러리) compatCheck 도구(kotlin-bcv, japicmp) 기록 여부.` → `(라이브러리) compatCheck 도구 기록 여부 — JVM은 kotlin-bcv·japicmp, Rust는 cargo-semver-checks 등 생태계 도구명을 그대로 기록한다.`
10. **모노레포 절 fixed dependents 무시**: 모노레포 config 기록 절(118행 근처)의 `fixed면 scope는 root 하나이고 versionLocations에 전 패키지 버전 파일이 들어간다.` → `fixed면 scope는 root 하나이고 versionLocations에 전 패키지 버전 파일이 들어간다(단일 scope이므로 \`dependents\`는 기록해도 무시된다 — 전파는 independent 전용).`

- [ ] **Step 2: references — 5건**

1. **edge-cases.md 회수 어휘**(`## 버전 재사용 금지` 절 끝, Docker 문단 뒤에 문단 추가):

```
회수가 필요할 때의 생태계별 어휘도 함께 알아두면 좋다 — npm deprecate·PyPI yank·cargo yank·Go retract(retract 지시문 추가 후 새 패치 발행)는 "지우지 않고 표시"하는 절차이고, 모바일 스토어는 롤백이 불가능해 수정판 롤포워드(필요시 단계 출시 중단)가 유일한 경로이며, 데스크톱 업데이터 피드는 직전 정상 버전을 재게시하고, 컨테이너 릴리스 태그는 재푸시 대신 새 패치 태그를 쓴다.
```

2. **prerelease-and-dev-channel.md immutableId SPA 어휘**(`immutableId` 목록 설명 문단(89행 근처) 끝에 이어서):

```
 정적 웹/SPA라면 불변 식별자는 빌드 해시가 담긴 산출물 파일명(`app.3f2a1c.js`)이나 빌드 시 주입되는 `<meta name="build">` 값이 같은 역할을 한다 — 별도 방식 이름은 없지만 config에는 자유 문자열로 기록해도 된다.
```

3. **notes-and-changelog.md KaC 헤딩 트레이드오프**(`## 조합` 절 앞에 문단 추가 또는 `## 목적지 4종` 절 끝):

```
모노레포의 루트 CHANGELOG가 쓰는 `## <scope>@<version>` 헤딩은 Keep a Changelog 규약 밖의 확장이다 — KaC를 기계 파싱하는 소비자는 드물어 실용상 문제가 없지만, 규약 준수가 필요하면 scope별 `package-changelog` 목적지(각 패키지의 CHANGELOG.md에 `## <version>` 표준 헤딩)를 쓰는 것이 대안이다.
```

4. **monorepo.md fixed dependents 무시**(fixed 설명 문단(81행 근처) 끝에 이어서):

```
 fixed에서는 scope가 하나뿐이므로 `dependents` 필드는 의미가 없다 — 기록돼 있어도 무시된다(전파는 independent 전용 개념이다).
```

5. **version-schemes.md Go /vN**(`## SemVer` 절 끝에 문단 추가):

```
Go 모듈은 v2부터 메이저 승급이 module path 개정(`module example.com/x/v2`)을 수반한다 — superrelease는 버전 파일과 태그만 관리하므로 path 개정은 수동 몫이다(go.mod 수정과 import 경로 갱신을 릴리스 전에 끝내라).
```

- [ ] **Step 3: 검증 + 커밋**

```bash
wc -l skills/init/SKILL.md
python3 -m unittest discover -s tests -q 2>&1 | tail -3
```

Expected: ≤500줄 / 전부 OK.

```bash
git add skills/init/SKILL.md skills/init/references
git commit -m "docs(init): B-19 프로즈 잔여 — 목적지·watchPaths·mergePolicy·PEP440·차등·회수 어휘·KaC·fixed·Go /vN·Phase 3 (B-19)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: README 양판 + CHANGELOG + 최종 검증

**Files:**
- Modify: `README.md`·`README_KO.md`·`CHANGELOG.md`

- [ ] **Step 1: README_KO.md — 4건**

1. config 표 `scopes[].notes.destinations` 행의 허용값에 ` \| package-changelog` 추가하고 비고에 `; package-changelog는 independent 모노레포 전용(scope 경로의 CHANGELOG.md)` 덧붙임.
2. config 표에 행 추가(`scopes[].preRelease.style` 행 근처, scope 필드 블록):

```
| `scopes[].watchPaths` | 경로 문자열 배열 | 선택; 이 경로들의 변경도 그 scope의 변경으로 집계(공유 코드·codegen 소스). independent 모노레포용 |
```

3. Known limits의 **Rust 불릿** 교체:

```
- **Rust** — `Cargo.toml` regex 위치는 동작하지만 `version.py set`이
  `Cargo.lock`을 갱신하지 않습니다(lockfile 동기화는 `package-lock.json`만).
```

→

```
- **Rust** — `Cargo.toml` regex 위치가 동작하고, `version.py set`이
  `Cargo.lock`의 자기 패키지 항목도 함께 갱신합니다(의존성 항목은 건드리지
  않음). Cargo workspace 멤버 순회는 지원하지 않습니다.
```

4. Known limits의 모바일 불릿(M10에서 지원 서술 위주가 된 것)을 케이스 커버리지 표 쪽으로 옮긴다: 커버리지 표의 적절한 행(레포 형태 또는 프로젝트 유형 행 인접)에 모바일 지원 요지(마케팅 축 감지 + 빌드 번호 CI-관리 모델)를 편입하고, 한계 절에는 다음 한 줄만 남긴다:

```
- **모바일 빌드 번호** — superrelease는 빌드 번호(`versionCode`,
  `CFBundleVersion`, pubspec `+N`) 값을 올리지 않습니다 — CI 몫입니다
  (`repo.buildNumber`가 관리 방식만 기록).
```

- [ ] **Step 2: README.md(영문) 대응 4건**

같은 4건을 영문판에 동일 적용(destinations 행·watchPaths 행·Rust 불릿·모바일 불릿 이동). Rust 교체문:

```
- **Rust** — `Cargo.toml` regex locations work, and `version.py set` also
  refreshes this package's own entry in `Cargo.lock` (dependency entries are
  never touched). Cargo workspace member traversal is not supported.
```

모바일 잔여 한 줄:

```
- **Mobile build numbers** — superrelease never bumps build-number values
  (`versionCode`, `CFBundleVersion`, pubspec `+N`) — that stays in CI
  (`repo.buildNumber` records only how they are managed).
```

- [ ] **Step 3: 골든 개수 32→33 갱신(양판)**

`grep -n "32종\|32 " README.md README_KO.md`로 대표 config 개수 의미의 32를 찾아 33으로(무관한 32는 유지). 골든 핀 목록 행에 ` · independent 모노레포 per-package CHANGELOG` / ` · independent monorepo per-package CHANGELOG` 항목 추가.

- [ ] **Step 4: CHANGELOG [Unreleased]**

`### Added` 끝에:

```markdown
- **Cargo.lock 동기화** — Rust 레포에서 `version.py set`이 `Cargo.lock`의
  자기 패키지 항목을 함께 갱신한다(의존성 항목 불변). 릴리스 커밋에 구버전
  lockfile이 박제되던 문제 해소.
- **scope별 watchPaths** — 공유 경로(공용 라이브러리·codegen 소스)의 변경을
  지정한 scope의 변경으로 집계한다. 전 모듈에 영향을 주는 변경이 릴리스
  감지에서 누락되던 축 해소.
- **per-package CHANGELOG 목적지** — independent 모노레포에서
  `package-changelog` 목적지로 각 패키지 경로의 CHANGELOG.md에 표준
  Keep a Changelog 헤딩(`## <version>`)으로 이력을 쌓을 수 있다
  (changesets 관행·npm tarball 동봉 대응, 대표 config 33종).
```

`### Fixed`(또는 `### Changed` — 성격에 맞게) 끝에:

```markdown
- **프로즈·감지 정합 일괄 정리** — 회수 어휘 확장(cargo yank·Go retract·스토어
  롤포워드·업데이터 피드·컨테이너), KaC 모노레포 헤딩 트레이드오프 명문화,
  fixed 전략의 dependents 무시 명시, Go `/vN` 안내, PEP 440 안내 트리거 보강,
  mergePolicy unknown 지침, preflight 결번 안내, scan 오탐 3건(비-project 테이블
  의존성·charts 중복·주석 versionName) 등 커버리지 리뷰 B-19 잔여를 소진했다.
```

- [ ] **Step 5: 최종 검증 + 커밋**

```bash
python3 -m unittest discover -s tests -q 2>&1 | tail -3
claude plugin validate . --strict 2>&1 | tail -1
git status --porcelain
```

Expected: 전부 OK / Validation passed / 이 태스크의 3개 파일만.

```bash
git add CHANGELOG.md README.md README_KO.md
git commit -m "docs: CHANGELOG M11 기입 + README Rust·watchPaths·package-changelog·모바일 한계 정리

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```
